<store>
  @type elasticsearch
  hosts "#{ENV['ELASTICSEARCH_HOSTS'] || ENV['ELASTICSEARCH_HOST'] + ':' + (ENV['ELASTICSEARCH_PORT'] || '9200')}"
  index_name "#{ENV['ELASTICSEARCH_INDEX_NAME']}"
</store>
