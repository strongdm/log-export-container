<store>
  @type mongo
  connection_string "mongodb://#{ENV['MONGO_USER']}:#{ENV['MONGO_PASSWORD']}@#{ENV['MONGO_HOST']}:#{ENV['MONGO_PORT']}/#{ENV['MONGO_DB']}"
  collection logs
  remove_tag_prefix class.
  <buffer>
    flush_interval 10s
  </buffer>
</store>
