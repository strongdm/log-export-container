<store>
  @type               kafka2
  brokers             host.docker.internal
  topic_key           topic

  <format>
    @type             json
  </format>
  # for more config options 
  # see https://github.com/fluent/fluent-plugin-kafka
</store>
